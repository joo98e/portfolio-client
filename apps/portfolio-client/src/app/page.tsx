import { FC } from "react";
import MainPage from "@/src/domain/home/components/pages/MainPage";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <MainPage />;
};

export default Page;
